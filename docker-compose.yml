services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY:-broadband_factsheet}:latest
    container_name: broadband-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=${DB_PATH}
    volumes:
      - ./data:/app/data
